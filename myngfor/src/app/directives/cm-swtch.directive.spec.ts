import { CmSwtchDirective } from './cm-swtch.directive';

describe('CmSwtchDirective', () => {
  it('should create an instance', () => {
    const directive = new CmSwtchDirective();
    expect(directive).toBeTruthy();
  });
});
