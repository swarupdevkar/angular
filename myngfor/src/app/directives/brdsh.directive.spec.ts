import { BrdshDirective } from './brdsh.directive';

describe('BrdshDirective', () => {
  it('should create an instance', () => {
    const directive = new BrdshDirective();
    expect(directive).toBeTruthy();
  });
});
